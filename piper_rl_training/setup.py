"""Setup script for piper_rl_training package."""

from setuptools import setup, find_packages

setup(
    name="piper_rl_training",
    version="1.0.0",
    description="RL training tasks for Piper robot in Isaac Lab",
    author="Your Name",
    packages=find_packages(),
    python_requires=">=3.10",
    install_requires=[],
)

